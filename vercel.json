{
  "version": 2,
  "installCommand": "python -m pip install -r requirements.txt",
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.py" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "src/**"
    }
  }
}
